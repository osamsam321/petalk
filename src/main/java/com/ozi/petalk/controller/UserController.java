package com.ozi.petalk.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
/*import org.springframework.security.core.userdetails.User;
*///import org.springframework.security.oauth2.provider.OAuth2Authentication;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.ozi.petalk.service.UserService;

@RestController
@RequestMapping("/petalk/user")
public class UserController {
	
	/*
	 * @Autowired UserService userService;
	 * 
	 * @GetMapping(value = "/oauth2.0/github") public ResponseEntity<User>
	 * LoginToPetalk(OAuth2Authentication authentication) { if(authentication !=
	 * null && authentication.isAuthenticated()) {
	 * 
	 * } return null;
	 * 
	 * }
	 */
	
	
}
