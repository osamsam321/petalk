import { Router } from "express";
import { OAUTH_ENDPOINT } from "../../constants/endpoint";
const router = Router();

router.get(OAUTH_ENDPOINT + "authenticate", (res:any, req:any) => {
    
    

    
});